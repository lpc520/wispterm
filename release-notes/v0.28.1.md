# Phantty v0.28.1

## Fixed

- Fixed Quake mode so the drop-down terminal no longer stays permanently
  topmost after it is shown.
- Fixed Quake mode visibility toggling so the user's current window frame is
  restored after hiding and showing the terminal.
- Fixed Phantty Remote Weixin bridge sync-buffer writes on Windows when multiple
  writes target the same file at once.
